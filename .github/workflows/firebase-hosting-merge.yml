# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on merge
"on":
  push:
    branches:
      - main
jobs:
  build_and_deploy_consumer:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: yarn workspace ftgo-consumer-web install --immutable
      - run: yarn workspace ftgo-consumer-web run build
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: "${{ secrets.GITHUB_TOKEN }}"
          firebaseServiceAccount: "${{ secrets.FIREBASE_SERVICE_ACCOUNT_FTGO_JANGJUNHA }}"
          channelId: live
          projectId: ftgo-jangjunha
          target: ftgo-consumer-web

  build_and_deploy_restaurant:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: yarn workspace ftgo-restaurant-web install --immutable
      - run: yarn workspace ftgo-restaurant-web run build
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: "${{ secrets.GITHUB_TOKEN }}"
          firebaseServiceAccount: "${{ secrets.FIREBASE_SERVICE_ACCOUNT_FTGO_JANGJUNHA }}"
          channelId: live
          projectId: ftgo-jangjunha
          target: ftgo-restaurant-web

  build_and_deploy_courier:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: yarn workspace ftgo-courier-web install --immutable
      - run: yarn workspace ftgo-courier-web run build
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: "${{ secrets.GITHUB_TOKEN }}"
          firebaseServiceAccount: "${{ secrets.FIREBASE_SERVICE_ACCOUNT_FTGO_JANGJUNHA }}"
          channelId: live
          projectId: ftgo-jangjunha
          target: ftgo-courier-web
